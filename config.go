package main

const (
	MONGODB_URI  = "mongodb://localhost"
	MONGODB_PORT = "27017"

	SERVER_PORT = "8080"
)

// environment:
// - MONGO_INITDB_ROOT_USERNAME=admin
// - MONGO_INITDB_ROOT_PASSWORD=password
